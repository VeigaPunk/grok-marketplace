diff --git a/crates/codegen/xai-grok-config-types/src/lib.rs b/crates/codegen/xai-grok-config-types/src/lib.rs
index 7fdc49b..80111b1 100644
--- a/crates/codegen/xai-grok-config-types/src/lib.rs
+++ b/crates/codegen/xai-grok-config-types/src/lib.rs
@@ -860,7 +860,8 @@ pub struct RemoteSettings {
     #[serde(default)]
     pub cancel_rewind_enabled: Option<bool>,
     /// Enables the session recap feature (`/recap` + automatic return-from-away).
-    /// Optional remote kill-switch; shell defaults ON when unset (set `false` to disable).
+    /// Livepatch ignores this remote flag (`FeatureSpec.remote = None`) and
+    /// defaults the shell feature OFF. Set `[features] session_recap = true` to opt in.
     #[serde(default)]
     pub session_recap: Option<bool>,
     /// Enables the session search index (`/load` deep search).
diff --git a/crates/codegen/xai-grok-config-types/src/registry.rs b/crates/codegen/xai-grok-config-types/src/registry.rs
index 420161c..1dff60c 100644
--- a/crates/codegen/xai-grok-config-types/src/registry.rs
+++ b/crates/codegen/xai-grok-config-types/src/registry.rs
@@ -108,8 +108,10 @@ pub const FEATURES: &[FeatureSpec] = &[
         key: "session_recap",
         path: "features.session_recap",
         env: "GROK_SESSION_RECAP",
-        default_enabled: true,
-        remote: Some(|settings| settings.session_recap),
+        // Livepatch: recap is substring ingest. Off unless the operator opts in.
+        default_enabled: false,
+        // Remote cannot turn recap back on (same pattern as ignoring a 4-slot pin).
+        remote: None,
     },
     FeatureSpec {
         id: Feature::AskUserQuestion,
diff --git a/crates/codegen/xai-grok-config-types/src/registry_tests.rs b/crates/codegen/xai-grok-config-types/src/registry_tests.rs
index d908059..befcee3 100644
--- a/crates/codegen/xai-grok-config-types/src/registry_tests.rs
+++ b/crates/codegen/xai-grok-config-types/src/registry_tests.rs
@@ -48,7 +48,7 @@ fn registered_settings() {
             ("session_search", ("GROK_SESSION_SEARCH", true)),
             ("lsp_tools", ("GROK_LSP_TOOLS", false)),
             ("web_fetch", ("GROK_WEB_FETCH", false)),
-            ("session_recap", ("GROK_SESSION_RECAP", true)),
+            ("session_recap", ("GROK_SESSION_RECAP", false)),
             ("ask_user_question", ("GROK_ASK_USER_QUESTION", true)),
             ("voice_mode", ("GROK_VOICE_MODE", true)),
             ("write_file", ("GROK_WRITE_FILE", true)),
diff --git a/crates/codegen/xai-grok-pager/src/notifications/config.rs b/crates/codegen/xai-grok-pager/src/notifications/config.rs
index 8230122..c4656de 100644
--- a/crates/codegen/xai-grok-pager/src/notifications/config.rs
+++ b/crates/codegen/xai-grok-pager/src/notifications/config.rs
@@ -10,9 +10,10 @@ pub struct NotificationConfig {
     pub sleep_prevention: bool,
     pub progress_bar: bool,
     /// Show an automatic "where was I" session recap when you return to the
-    /// terminal after being away. Only applies when the shell has rolled out
-    /// session recap (`sessionRecap` on ACP initialize / remote settings). Manual
-    /// `/recap` is gated by the shell flag alone, not this toggle.
+    /// terminal after being away. Default OFF. Only applies when the shell
+    /// feature `[features] session_recap` is on (`sessionRecap` on ACP
+    /// initialize). Remote settings cannot re-enable it. Manual `/recap` is
+    /// gated by the shell flag alone, not this toggle.
     pub session_recap: bool,
     /// Minimum seconds the terminal must be unfocused ("stepped away") before
     /// the client requests an automatic recap. A short debounce against quick
@@ -35,7 +36,7 @@ impl Default for NotificationConfig {
             ],
             sleep_prevention: true,
             progress_bar: true,
-            session_recap: true,
+            session_recap: false,
             session_recap_threshold_secs: 30,
             title: TitleConfig::default(),
             hooks: Vec::new(),
@@ -164,10 +165,11 @@ events = [\"turn_complete\", \"approval_required\"]
 sleep_prevention = true
 # Show a progress indicator in the terminal tab (OSC 9;4).
 progress_bar = true
-# Show an automatic \"where was I\" session recap when you return after being away.
-# Shell session_recap is on by default; disable via [features] session_recap or
-# GROK_SESSION_RECAP=0. Manual /recap uses only the shell flag.
-session_recap = true
+# Automatic \"where was I\" recap on return-from-away. Default OFF.
+# Recap is substring ingest; livepatch keeps it off. Remote cannot re-enable.
+# Opt in only with [features] session_recap = true and this key true.
+# Manual /recap uses only the shell [features] session_recap flag.
+session_recap = false
 # Minimum seconds unfocused (\"stepped away\") before requesting a recap; a
 # debounce against quick tab blips. The \"3 min since the last turn\" timing is
 # enforced agent-side.
@@ -242,7 +244,7 @@ mod tests {
         );
         assert!(parsed.sleep_prevention);
         assert!(parsed.progress_bar);
-        assert!(parsed.session_recap);
+        assert!(!parsed.session_recap);
         assert_eq!(parsed.session_recap_threshold_secs, 30);
         assert!(parsed.title.enabled);
         assert!(parsed.hooks.is_empty());
diff --git a/crates/codegen/xai-grok-pager/tests/pty_e2e/recap_header_not_in_selection_pty.rs b/crates/codegen/xai-grok-pager/tests/pty_e2e/recap_header_not_in_selection_pty.rs
index 8cc1c96..ebcdbff 100644
--- a/crates/codegen/xai-grok-pager/tests/pty_e2e/recap_header_not_in_selection_pty.rs
+++ b/crates/codegen/xai-grok-pager/tests/pty_e2e/recap_header_not_in_selection_pty.rs
@@ -69,7 +69,7 @@ async fn recap_header_not_in_selection_pty() {
         "We fixed text selection so only {RECAP_BODY_TOKEN} is copyable."
     ));
 
-    // Manual /recap — session_recap is ON by default in the shell.
+    // Manual /recap — this harness opts the shell feature on; stock default is OFF.
     harness.inject_keys(b"/recap\r").expect("submit /recap");
     harness
         .wait_for_text(RECAP_BODY_TOKEN, Duration::from_secs(45))
diff --git a/crates/codegen/xai-grok-shell/src/extensions/recap.rs b/crates/codegen/xai-grok-shell/src/extensions/recap.rs
index cb0da27..7c31015 100644
--- a/crates/codegen/xai-grok-shell/src/extensions/recap.rs
+++ b/crates/codegen/xai-grok-shell/src/extensions/recap.rs
@@ -30,9 +30,9 @@ pub async fn handle(agent: &MvpAgent, args: &acp::ExtRequest) -> ExtResult {
     let req: RecapRequest = parse_params(args)?;
     tracing::info!(auto = req.auto, "handling /recap request");
 
-    // Feature gate: remote setting / `[features] session_recap`
-    // config.toml key / `GROK_SESSION_RECAP` env (default ON). Gates both the
-    // manual `/recap` and the automatic recap.
+    // Feature gate: `[features] session_recap` / `GROK_SESSION_RECAP` (default OFF).
+    // Gates both the manual `/recap` and the automatic return-from-away recap.
+    // Recap is substring ingest; livepatch keeps it off. Remote cannot re-enable.
     if !agent.cfg.borrow().is_session_recap_enabled() {
         tracing::debug!("session recap disabled by config/feature flag; ignoring request");
         return to_ext_response(Ok(serde_json::json!({ "ok": true, "disabled": true })));
