---
description: Fork a child xbgst round under this L1. Does not create a second judge. Crossbreed / xask-first.
argument-hint: <disjoint task for a child planner>
---

# /xbgst-orch

Fork a **child orch** under the current L1 (the judge in this Grok session). **mode: xbgst**.

This is not a second L1. Do **not** spawn type xbgst. Do **not** run Pareto / `APPROVED` / ship in the child. Spawn `the-planner` for `$ARGUMENTS` as a sibling wave. Child returns are **evidence** for L1.

Use when the user flags a disjoint task, or when L1 already decided to fork. Same substrate: in-process `spawn_subagent`, fnm-multishell. Host ceiling 64.

Load skill **xbgst** concurrent-child-orch rules. PATH `xask` first: every gx-* FIRST is Cursor Ultra gravy `xask --provider cursor --model-id kimi-k3-max --gs`. Named routes `xask --gs ds-pro`, `xask --gs kimi`, and `xask --gs qwen38` are not hangar default. Never spawn type `xask`. Never `general-purpose` or `explore`.
