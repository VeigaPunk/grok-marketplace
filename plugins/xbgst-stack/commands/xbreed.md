---
description: Grok godspeed pipeline (alias of /xbgst). Crossbreed / xask-first. No general-purpose fallback.
argument-hint: <task>
---

# /xbreed

Same as **`/xbgst`** (xbgst-mode / xask-first). Load skill **xbgst** and execute `$ARGUMENTS`.

**Do not** fall back to `subagent_type=general-purpose`. Spawn named specialists only. Specialists FIRST call PATH `xask`.
