---
description: Grok godspeed pipeline (alias of /xbgst). No general-purpose fallback.
argument-hint: <task>
---

# /xbreed

Alias for **`/xbgst`**. Load skill **xbgst** and execute `$ARGUMENTS`.

**Do not** fall back to `subagent_type=general-purpose`. Spawn named specialists only.
