---
description: Slash clone of /xbgst. Loads skill xbgst. Crossbreed / xask-first. No Claude TeamCreate.
argument-hint: <task>
---

# /xbreed-team

Load skill **xbgst** with `$ARGUMENTS`. **mode: xbgst**. Crossbreed / xask-first (crossbreed).

Slash clone of **`/xbgst`** (SSoT). Both load the one skill **xbgst**. `/xgs` is the native-only sibling (no xask).

Grok host: spawn named specialists only. No `~/.claude/*`. PATH `xask` first with flags that name the target CLI. Default FIRST consult is Cursor Ultra gravy for scout/connector/the-planner (`xask --provider cursor --model-id kimi-k3-max --gs`) and Token Plan DeepSeek for implement/review (`xask --gs ds-pro` per role table). Named routes `xask --gs kimi` (native Kimi Code OAuth) and `xask --gs qwen38` (Token Plan) are not hangar default. `cdx` is OpenAI-only (`xask --gs --service-tier fast cdx`; `--gpt55`; `--spark` → sekhmet/`codex-titanium` / `service_tier=fast`). `--spark` is L3 opt-in. Never spawn type `xask`. Never `xask-l3` as FIRST.

Consult table: `commands/references/xbreed-shared.md`.
