---
description: Grok godspeed team mode (alias of /xbgst). Cross-model xask optional only if user asks.
argument-hint: <task>
---

# /xbt

On Grok Build this is an alias of **`/xbgst`** / skill **xbgst**.

- Default: Grok specialists only (no xask gate).
- If the user explicitly requests cross-model and `xask` exists on PATH, optional single-shot consults may be used; never block on missing xask.

`$ARGUMENTS` is the task.
